<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atualizar Produtos</title>
</head>

<body>
    <h1>Atualizar Produtos</h1>

    <form method="post" action="/produtos/atualizar/<%= produto?.id %>">
        <div>
            <label for="name">Nome</label>
            <input type="text" id="name" name="name" value="<%= produto?.name %>">
        </div>

        <div>
            <label for="codigo">Codigo</label>
            <input type="text" id="codigo" name="codigo" value="<%= produto?.codigo %>">
        </div>

        <div>
            <label for="categoria">categoria</label>
            <input type="text" id="categoria" name="categoria" value="<%= produto?.categoria %>">
        </div>

        <div>
            <label for="preco">Preço</label>
            <input type="numero" id="preco" name="preco" value="<%= produto?.preco %>">
        </div>

        <div>
            <label for="quantidade">Quantidade</label>
            <input type="numero" id="quantidade" name="quantidade" value="<%= produto?.quantidade %>">
        </div>

        <div>
            <label for="status">Status</label>
            <select id="status" name="status" value="<%= produto?.status %>">
                <option value="Ativo">Ativo</option>
                <option value="Inativo">Inativo</option>
            </select>
        </div>

        <div>
            <input type="submit" value="Atualizar">
        </div>
    </form>

    <ul>
        <LI><a href="/">Home</a></LI>
        <li><a href="/produtos">Lista de Produtos</a></li>
        <li><a href="/produtos/cadastrar">Cadastrar Produto</a></li>
        <li><a href="/produtos/exibir/<%= produto.id %>">Exibir Produto</a></li>
        <li><a href="/produtos/deletar/<%= produto.id %>">Deletar produto</a></li>
    </ul>

</body>

</html>